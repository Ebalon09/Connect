{% extends "layout.html.twig" %}

{% block title %}Twitter Feed{% endblock %}

{% block input %}
{{ include('tweet/tweetInput.html.twig') }}
{% endblock %}

{% block content %}
    <!--here foreach with user posts,names,profile pics and buttons-->
    <label for="Text" class="sr-only">Posts</label>
    {% for data in result %}
        <div class="media">
            <a class="media-left" href="#fake">
                <img id="userpicpost" alt="" class="media-object img-rounded" src="{{ data.User.Picture }}">
            </a>
            <div class="media-body">
                <h4 class="media-heading" id="Username">{{ data.User.Username }}</h4>
                <p id="Text">{{ data.Text }}</p>
                {% if(data.LinkId is not same as(null)) %}
                    <iframe id="yt" width="400" height="200" src="https://www.youtube.com/embed/{{ data.LinkID }}" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
                {% endif %}
                {% if(data.Retweet is not same as(null)) %}
                    <div class="media-body">
                        <h4 class="media-heading" id="Username">{{ data.ReTweet.User.Username }}</h4>
                        <p id="Text">{{ data.ReTweet.Text }}</p>
                    </div>
                {% endif %}
                {% if(userid is not same as(null)) %}
                <ul id="buttons" class="nav nav-pills nav-pills-custom">
                    <li><a id="comment" href="./index.php?controller=CommentController&action=indexAction&id={{ data.Id }}&idc={{ data.Id }}&c=true" ><i class="far fa-comment-alt"></i></a></li>
                    <li><a id="commentCounter" href="./index.php?controller=CommentController&action=commentFeed&id={{ data.Id }}&c=true">{{ countcomments[data.Id] }}</li>
                    <li><a id="reTweet" href="./index.php?controller=TwitterController&action=reTweetAction&id={{ data.Id }}&idc={{ data.Id }}"><i class="fas fa-retweet"></i> </a></li>

                    {% if(data.isLikedByUser(likes)) %}
                        <li><a id="dislike" href="./index.php?controller=LikeController&action=dislikeAction&id={{ data.Id }}&idc={{ data.Id }}"><i class="fas fa-heart"></i> </a></li>
                        <li><a href="#" id="likeCounter">{{ countlikes[data.Id] }}</li>
                    {% else %}
                        <li><a id="like" href="./index.php?controller=LikeController&action=likeAction&id={{ data.Id }}&idc={{ data.Id }}"><i class="far fa-heart"></i> </a></li>
                        <li><a href="#" id="likeCounter">{{ countlikes[data.Id] }}</li>
                    {% endif %}
                    {% if(userid == data.getUser().Id) %}
                        <li><a id="delete" href="./index.php?controller=TwitterController&action=deleteAction&id={{ data.Id }}"><i class="far fa-trash-alt"></i></a></li>
                        <li><a id="edit" href="./index.php?controller=TwitterController&action=updateAction&id={{ data.Id }}"><i class="far fa-edit"></i> </a></li>
                    {% endif %}
                    {% endif %}
                    {% if(c == true) %}
                        <form action="./index.php?controller=CommentController&action=createAction&id={{ data.id }}" method="POST">
                            <input id="commentinput" name="text" type="text" class="form-control" autocomplete="off">
                        </form>
                    {% endif %}
                </ul>
            </div>
        </div>
        <hr>
    {% endfor %}
{% endblock %}